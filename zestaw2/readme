Zadanie poolega na stworzeniu procesu funkjcją fork() i zastąpieniu go innym programem o nazwie podpoogram który wypisuje odpowiednie indentyfikatory. Do wykonania programu uyto funkcji execl, która jako pierwszy argument dostaje komendę do wykonania a następnie argumenty dodatkowe. przy n wywołaaniach forka tworzy się n procsów potomnych, więc łącznie z proceseem macierzystym otrzymujemy n+1 procesów.